[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = jmortty
	email = jmortty@hawk.iit.edu
[init]
	default = branch
